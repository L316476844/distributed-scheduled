<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd

                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd

                           http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.0.xsd"
       default-autowire="byName" default-lazy-init="false">


    <!-- zk配置 -->
    <bean id="zkConfig" class="com.dangdang.ddframe.job.reg.zookeeper.ZookeeperConfiguration">
        <constructor-arg name="namespace" value="${elastic.job.registry.namespace}" />
        <constructor-arg name="serverLists" value="${elastic.job.registry.zks}"/>
        <property name="maxRetries" value="${elastic.job.max-retries}"/>
        <property name="maxSleepTimeMilliseconds" value="${max.sleep.time.milliseconds}"/>
        <property name="baseSleepTimeMilliseconds" value="${base.sleep.time.milliseconds}"/>
    </bean>

    <!-- 配置作业注册中心 -->
    <bean id="zkRegistryCenter" class="com.dangdang.ddframe.job.reg.zookeeper.ZookeeperRegistryCenter" init-method="init">
        <constructor-arg name="zkConfig" ref="zkConfig"/>
    </bean>



</beans>